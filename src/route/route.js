const routesAll = {
  GUEST: {
    routes: [{ path: '/', component: '' }],
    redirect: '/',
  },
  CUSTOMER: {
    routes: [{ path: '/', component: '' }],
    redirect: '/',
  },
  ADMIN: {
    routes: [{ path: '/', component: '' }],
    redirect: '/',
  },
};

export default routesAll;
